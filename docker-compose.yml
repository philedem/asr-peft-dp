version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/workspace/data
      - huggingface-cache:/workspace/.cache/huggingface  # Cache models across rebuilds
    environment:
      - PYTHONUNBUFFERED=1
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
    environment:
      - VITE_BACKEND_URL=http://localhost:8000

volumes:
  huggingface-cache:  # Named volume for model cache persistence