# Core web/backend
fastapi
uvicorn[standard]
pydub
python-multipart

# CORS for development
starlette

# Machine Learning
# Note: torch, transformers, peft, datasets, trl, bitsandbytes installed in Dockerfile
jiwer
mlflow
optuna

# Differential privacy
opacus

# For handling HTTP requests in LoRA training script
requests

# Audio
soundfile
librosa

# WhisperLive for real-time transcription (commented out for now - will install separately)
# git+https://github.com/collabora/WhisperLive.git

# Data processing
numpy
pandas

# Progress bars
tqdm
