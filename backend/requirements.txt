# Core web/backend
fastapi
uvicorn[standard]
pydub
python-multipart

# CORS for development
starlette

# Machine Learning
# Note: torch and torchaudio are installed in Dockerfile with CUDA support
transformers
datasets
jiwer
mlflow
optuna
tqdm

# LoRA / Parameter-Efficient Fine-Tuning
peft

# Fast GPU inference with CTranslate2 (built from source in Dockerfile with CUDA support)
faster-whisper==1.0.3

# Differential privacy
opacus

# For handling HTTP requests in LoRA training script
requests

# Audio
soundfile

# Data processing
numpy
pandas

# Progress bars
tqdm
